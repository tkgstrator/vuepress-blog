<template><h2 id="ipswitch" tabindex="-1"><a class="header-anchor" href="#ipswitch" aria-hidden="true">#</a> <a href="https://github.com/3096/ipswitch/releases" target="_blank" rel="noopener noreferrer">IPSwitch<OutboundLink/></a></h2>
<p>IPSwitch とは pchtxt（パッチテキスト）を読み込み、IPS 形式のパッチとして出力するツールのこと。</p>
<p>これがあるだけでわざわざ自分で IPS ファイルを書かなくてもコードを実際に使える形式に変換してくれます。</p>
<p>まずは IPSwitch をダウンロードし、switch フォルダ内に「ipswitch」というフォルダを作成し、ipswitch.nro をコピーします。</p>
<p>つぎに ipswitch フォルダ内に名前は何でもいいので、フォルダを作成します。</p>
<p>あとはそのつくったフォルダの中に pchtxt 形式のコードファイルをコピーします。</p>
<p>例えとしては、こんな感じです。</p>
<p><code>sdmc:/switch/ipswitch/Splatoon 2/5.4.0public.pchtxt</code></p>
<p>コードは移植と開発の二通りの入手方法があります。</p>
<p>また、インターネットで誰かが公開しているものを使うという手も考えられます。</p>
<p><a href="https://takaharu422.github.io/Splatoon2.github.io/ja.html" target="_blank" rel="noopener noreferrer">スプラトゥーン2 チートコード<OutboundLink/></a></p>
<h2 id="switch-側の設定" tabindex="-1"><a class="header-anchor" href="#switch-側の設定" aria-hidden="true">#</a> Switch 側の設定</h2>
<p>hbmenu から IPSwitch を起動します。</p>
<p>Toggle Patch Text Contents で A ボタンを押して進めます。</p>
<p>今回は 5.4.0 向けのコードを有効化したかったので 5.4.0public.pchtxt で A ボタンを押すと、ファイル内に記述されているコードがロードされます。</p>
<p><img src="https://pbs.twimg.com/media/E2cR9kiVUAA3qT6?format=png" alt=""></p>
<p>赤色表示は無効化されているコードなので有効化したいもので A ボタンを押して逐次好きなようにカスタマイズしてください。</p>
<p>カスタマイズができたら最後に Y ボタンを押して IPS ファイルを出力します。</p>
<p><img src="https://pbs.twimg.com/media/E2cR_wnVEAIeDJG?format=png" alt=""></p>
<p>All Done と表示されたらコード出力が完了したので + ボタンで hbmenu に、HOME ボタンで HOME メニューに戻れます。</p>
<p>あとは好きなだけ遊んでください。</p>
<p>記事は以上。</p>
</template>
